import { p, q, pp, qq } from "@src/design/index2"

describe("it-people", () => {
    test("ok", async () => {
        p // ?
        q // ?
        pp // ?
        qq // ?
        expect(1).toBe(1)
    })
})